<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thtf.op.mapper.FileUploadRecordMapper">

    <select id="getByFileId" resultType="com.thtf.emdedding.dto.FileUploadRecordDTO">
        select * from file_upload_record where guid = #{fileId}
    </select>
</mapper>